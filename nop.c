#include "monty.h"
/**
  *func_nop- does nothing
  *@headers: stack head
  *@counts: line_number
  *Return: no return
 */
void func_nop(stack_t **headers, unsigned int counts)
{
	(void) counts;
	(void) headers;
}
