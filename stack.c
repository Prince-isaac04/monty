#include "monty.h"
/**
 * func_stack - prints the top
 * @head: stack head
 * @counts: line_number
 * Return: no return
 */
void func_stack(stack_t **head, unsigned int counts)
{
	(void)head;
	(void)counts;
	bus.lifi = 0;
}
